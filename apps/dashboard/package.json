{
  "name": "dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../.env.local bun run next dev -- -p 3000",
    "dev:test": "dotenv -e ../../.env.test next dev -- -p 3000",
    "dashboard": "dotenv -e ../../.env.local bun run next dev -- -p 3000",
    "build": "next build",
    "build:dev": "next build",
    "start": "next start",
    "clean": "rm -rf node_modules .turbo .next",
    "lint": "next lint",
    "tsc": "npx tsc --noEmit"
  }
}
